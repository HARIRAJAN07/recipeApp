

# Recipe Data Collection and REST API

This project parses a JSON file containing recipe data, stores it in a PostgreSQL database, and exposes REST APIs to create and retrieve recipes.

The application loads data automatically on startup and allows querying the top-rated recipes.

---

## Tech Stack

- Java 17
- Spring Boot
- PostgreSQL
- Maven

---

## Project Structure
